


import { createSlice } from '@reduxjs/toolkit';


const dateSlice = createSlice({
    name: 'dateSlice',
    initialState: {
        selectedDate: ""
    },
    reducers:{
        
    }
});

export default dateSlice.reducer;